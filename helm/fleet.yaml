defaultNamespace: velero

helm:
  releaseName: velero

targetCustomizations:
- name: k8s-us-east
  helm:
    valuesFiles:
    - values/k8s-us-east-values.yaml
  clusterSelector:
    matchLabels:
      clusterName: k8s-us-east